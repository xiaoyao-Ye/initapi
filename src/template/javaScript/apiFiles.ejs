/* eslint-disable */
// 此文件由 initAPI(https://github.com/xiaoyao-Ye/initapi) 自动生成。请不要手动修改此文件！

<% if (description) { %>
// Current controller description:
<%= description %>
<% } %>

<%- importRequest %>

<% funcList.forEach((item) => { %>
<%= item.fnDescription %>
export const <%= item.fnName %> = (<%- item.fnArgs %>) => {
  return <%= useRequest %>({
    url: `<%- item.url %>`,
    method: '<%= item.fnMethod %>',
    <%- item.fnRequestBody %>
  });
}
<% }) %>