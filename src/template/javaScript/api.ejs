/* eslint-disable */
// 此文件由 initAPI(https://github.com/xiaoyao-Ye/initapi) 自动生成。请不要手动修改此文件！

<%- importAxios %>

<% apiList.forEach(({tagName, desc, funcList}) => { %>
<%= desc %>
export class <%= tagName %> {
  <% funcList.forEach((item) => { %>
    <%= item.desc %>
    static async <%= item.funcName %>(<%- item.args %>) {
      return <%= useAxios %>({
        url: `<%- item.url %>`,
        method: '<%= item.method %>',
        <%- item.req %>
      })
    }
  <% }) %>
}
<% }) %>