/* eslint-disable */
// 此文件由 initAPI(https://github.com/xiaoyao-Ye/initapi) 自动生成。请不要手动修改此文件！

<%- importAxios %>
import { <%= importEntityName %> } from './entity';

<% apiList.forEach(({className, funcList}) => { %>
export class <%= className %> {
  <% funcList.forEach((item) => { %>
    <%= item.desc %>
    static async <%= item.funcName %>(<%- item.args %>) {
      return <%= item.use %><<%- item.res %>>({
        url: `<%- item.url %>`,
        method: '<%= item.method %>',
        <%- item.req %>
      })
    }
  <% }) %>
}
<% }) %>