/* eslint-disable */
// 此文件由 initAPI(https://github.com/xiaoyao-Ye/initapi) 自动生成。请不要手动修改此文件！

<%- importRequest %>
<% if (importAllType) { %>
import { <%= importAllType %> } from './typings.d';
<% } %>

<% controllerList.forEach(({controllerName, description, funcList}) => { %>
<%= description %>
export class <%= controllerName %> {
  <% funcList.forEach((item) => { %>
    <%= item.fnDescription %>
    static <%= item.fnName %>(<%- item.fnArgs %>) {
      return <%= useRequest %><<%- item.fnResponse %>>({
        url: `<%- item.url %>`,
        method: '<%= item.fnMethod %>',
        <%- item.fnRequestBody %>
      });
    }
  <% }) %>
}
<% }) %>